get:
  tags:
    - WeatherForecast
  responses:
    '200':
      description: Success
      content:
        text/plain:
          schema:
            type: array
            items:
              $ref: ..\components\schemas\WeatherForecast.yaml
        application/json:
          schema:
            type: array
            items:
              $ref: ..\components\schemas\WeatherForecast.yaml
        text/json:
          schema:
            type: array
            items:
              $ref: ..\components\schemas\WeatherForecast.yaml
